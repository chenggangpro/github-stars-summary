spring:
  application:
    name: connect-example-client
  flyway:
    enabled: false
  jackson:
    locale: zh_CN
    time-zone: "Asia/Shanghai"
  config:
    import: optional:classpath:github-token.properties[.properties]
logging:
  level:
    pro.chenggang: DEBUG